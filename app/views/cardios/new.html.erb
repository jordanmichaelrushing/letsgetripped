<% title "- New Cardio" %>
<h1>New Cardio</h1>

<%= form_for(@cardio) do |f| %>
  <%= f.hidden_field :day_id, value: @day_id %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Back', day_path(@day_id) %>