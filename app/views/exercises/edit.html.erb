<h1>Editing Exercise</h1>

<%= form_for(@exercise) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Show', "/exercises/#{@exercise.id}?day=#{@day_id}" %> |
<%= link_to 'Back', exercise_path(@exercise, day: @day_id) %>